## Packages
recharts | For rich dashboard analytics and progress visualization
date-fns | For formatting dates in tables and notifications
lucide-react | Standard icon set for UI elements

## Notes
The application expects standard JWT/Session cookie-based auth handling on the backend.
Wouter handles routing, using Link without nested <a> tags.
Tailwind is configured with CSS variables for colors.
Icons are standard lucide-react.
